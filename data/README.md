# Domain knowledge base

Place plain-text documents per domain:

```
data/
  {domain_id}/
    document1.txt
    document2.txt
```

After adding or changing files, reindex: `python scripts/reindex_rag.py`
