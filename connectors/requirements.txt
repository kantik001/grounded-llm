# Optional deps for live Google Drive API connector.
# SharePoint / Confluence live use `requests` (already in api/requirements.txt).
#
#   pip install -r connectors/requirements.txt
#
# Alias kept for older docs/scripts: api/requirements-connectors.txt

google-api-python-client>=2.200.0
google-auth>=2.57.0
