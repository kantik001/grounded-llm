# ⚠️ Устаревшее имя / Deprecated doc name

Документ перенесён → **[rag-domains_config.md](./rag-domains_config.md)**

Термин **`crop_id`** заменён на **`domain_id`**.  
Конфиг: `config/domains.json` (legacy: `crops.json`).

Файл кода: `rag/domains_config.py` (не `crops_config.py`).
