Thank you for installing {{ .Chart.Name }}.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Components:
  - Go API:     {{ include "grounded-llm.fullname" . }}-server:{{ .Values.server.port }}
  - Python RAG: {{ include "grounded-llm.fullname" . }}-python:{{ .Values.python.port }} (gRPC :{{ .Values.python.grpcPort }})
  {{- if .Values.webapp.enabled }}
  - Web UI:     {{ include "grounded-llm.fullname" . }}-webapp:{{ .Values.webapp.port }}
  {{- end }}

Health checks:
  - Liveness:  GET /health  (Go), GET /health (Python)
  - Readiness: GET /ready   (Go probes DB + Python; Python probes data/chroma)

Before production:
  1. Use values-prod.example.yaml (or equivalent); set secrets via values or external Secret manager
  2. Mount knowledge-base data and config (see docs/en/K8S_DEPLOY.md)
  3. Bring Redis yourself if you need caches (REDIS_URL) — not deployed by this chart
  4. Configure ingress TLS and backup schedule (docs/en/BACKUP_RESTORE.md)

Documentation: deploy/README.md · docs/en/K8S_DEPLOY.md · docs/en/TRUST_CENTER.md
